mutation PauseAllEventTimers($roundId: ID!) {
  roundPauseAllTimers(roundId: $roundId) {
    timers {
      ...TimerListItem
    }
  }
}

mutation UnpauseAllEventTimers($roundId: ID!) {
  roundUnpauseAllTimers(roundId: $roundId) {
    timers {
      ...TimerListItem
    }
  }
}
