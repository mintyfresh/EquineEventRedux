mutation CreateTimerPreset($input: TimerPresetCreateInput!) {
  timerPresetCreate(input: $input) {
    timerPreset {
      id
      ...TimerPresetListItem
    }
    errors {
      ...Errors
    }
  }
}
