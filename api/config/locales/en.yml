en:
  activerecord:
    attributes:
      match: &match
        player1: Player 1
        player2: Player 2
      round/matches:
        <<: *match
        table: Table number
    errors:
      models:
        match:
          attributes:
            player1:
              same_as_player2: cannot also be Player 2
              required: must be selected
